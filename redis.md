# Redis 开发笔记

## 简介

Redis 是一款开源的内存数据结构存储系统，以其高性能、高并发支持而著称。它不仅能够作为数据库使用，还可以充当缓存和消息队列中间件。由于其数据存储于内存中，因此在读写性能、可扩展性及灵活性方面表现优异。

## 使用 Redis 的优势

- **提升读写速度**：通过将数据保存在内存中，极大地提高了数据处理速度。
- **增强并发能力**：高效管理大量并发请求的能力，使其成为构建高并发应用的理想选择。

## 支持的数据类型

Redis 支持多种数据类型，每种类型都针对特定的应用场景进行了优化：

- **字符串 (String)**：最基本的数据类型，适用于简单的键值对存储。
- **散列表 (Hash)**：用于存储对象，允许字段和字段值之间的映射。
- **链表 (List)**：一个双向链表，支持从两端快速插入或移除元素。
- **集合 (Set)**：无序且不重复的元素集合，支持多种集合操作，如交集、并集等。
- **有序集合 (Sorted Set)**：与集合类似，但每个元素关联一个分数，通过分数来为成员排序。

## 命令参考

### 字符串 (String) 操作

- `SET key value` - 设置指定键的值。
- `GET key` - 获取指定键的值。
- `INCR key` - 将键存储的数字值增一。
- `DECR key` - 将键存储的数字值减一。

### 散列表 (Hash) 操作

- `HSET key field value` - 将哈希表key中的字段field的值设为value。
- `HGET key field` - 获取存储在哈希表中指定字段的值。
- `HGETALL key` - 获取在哈希表中指定key的所有字段和值。

### 链表 (List) 操作

- `LPUSH key value` - 在列表的左侧插入一个值。
- `RPUSH key value` - 在列表的右侧插入一个值。
- `LPOP key` - 移出并获取列表的第一个元素。
- `RPOP key` - 移出并获取列表的最后一个元素。

### 集合 (Set) 操作

- `SADD key member` - 向集合添加一个或多个成员。
- `SMEMBERS key` - 返回集合中的所有成员。
- `SINTER key1 key2 ...` - 返回给定所有集合的交集。

### 有序集合 (Sorted Set) 操作

- `ZADD key score member` - 向有序集合添加一个或多个成员，或者更新已存在成员的分数。
- `ZRANGE key start stop [WITHSCORES]` - 返回有序集中，指定区间内的成员。
- `ZREM key member` - 从有序集合中删除一个或多个成员。

### 事务

- `MULTI` - 标记一个事务块的开始。
- `EXEC` - 执行所有事务块内的命令。
- `DISCARD` - 取消事务，放弃执行事务块内的所有命令。

### 发布/订阅

- `SUBSCRIBE channel [channel ...]` - 订阅给定的一个或多个频道的信息。
- `PUBLISH channel message` - 将信息发送到指定的频道。

### 持久化选项

- **RDB (Redis DataBase)**：一种快照机制，默认情况下每隔一段时间会生成当前数据的一个快照。
- **AOF (Append Only File)**：记录服务器接收到的每个修改数据集的命令。可以配置不同的同步策略（例如每秒同步一次）以平衡性能和数据安全性。

更多命令及其详细用法，请参阅[官方文档](https://redis.io/commands)。
